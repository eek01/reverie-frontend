import "../css/Home.css";

const Home = () => {
    return (
        // use className instead of class bc is a key word
        <main id="home" className="main-content">
            <h1>Home</h1>
        </main>
    );
};

export default Home; 